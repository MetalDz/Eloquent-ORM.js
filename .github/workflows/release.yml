name: Release

on:
  push:
    branches:
      - ai_master
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish
    uses: ./.github/workflows/publish.yml
    secrets: inherit
