FROM node:20-bookworm

WORKDIR /workspace

COPY package*.json ./
RUN npm ci

COPY . .

ENV CI=true
ENV FORCE_COLOR=0
ENV ELOQUENT_RUNTIME_LOG=false
ENV DOTENV_CONFIG_QUIET=true
ENV APP_ENV=development
ENV ELOQUENT_PACK_SMOKE_ENABLE_MONGO_RUNTIME=1

CMD ["npm", "run", "test:pack-smoke"]
