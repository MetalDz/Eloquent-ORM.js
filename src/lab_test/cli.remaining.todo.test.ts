describe("CLI remaining coverage backlog", () => {
  test.todo("migrate:run: extend app-mode --pivot-separate shell coverage to mysql and pg for full parity");
  test.todo("db:seed:fresh: add standalone mysql/pg single-connection shell coverage in test mode if desired");
  test.todo("demo:scenario: add explicit app-mode --random shell coverage beyond the current --user coverage");
});
